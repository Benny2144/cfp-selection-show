{
  "name": "cfp-dynasty-studio",
  "version": "3.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.9.0",
  "scripts": {
    "build": "python tools/make_site.py && node tools/bundle_site.mjs",
    "check": "node --check worker.js && node --check js/cloud.js && node --check js/app.js && node --check js/logos.js && node --check js/show.js && node --check js/dynasty.js && node --check js/pickem.js && node --check js/ui-dynasty.js && node --check js/teams.js && node --check sw.js",
    "test": "vitest run",
    "verify": "pnpm run check && pnpm test && pnpm run build && pnpm run deploy:check",
    "deploy": "pnpm run build && wrangler deploy",
    "deploy:check": "wrangler deploy --dry-run"
  },
  "dependencies": {
    "jose": "6.2.7"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.20.1",
    "@types/node": "26.1.2",
    "esbuild": "0.28.1",
    "vitest": "^4.1.10",
    "wrangler": "4.118.0"
  }
}
